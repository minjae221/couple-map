# 우리의 기억 (couple-map)

둘이 함께 간 곳(지도 핀 + 사진 + 한 줄)과 버킷리스트를 모으는 PWA.

- 지도: OSM/CARTO (전세계) · 검색: 카카오(국내) + Nominatim(해외)
- 데이터: 브라우저 localStorage (설정에서 JSON 백업/불러오기)
- 배포: `./deploy.sh "메시지"` → GitHub Pages 자동 반영
